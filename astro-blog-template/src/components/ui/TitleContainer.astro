---
const { headingLevel = "h1", className = "" } = Astro.props;
const Tag = headingLevel;
---

<Tag class={`text-5xl text-start font-bold tracking-tight text-zinc-300 sm:text-4xl ${className}`}>
  <slot />
</Tag>