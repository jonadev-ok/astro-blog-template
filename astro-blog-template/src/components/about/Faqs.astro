---
const faqs = [
  {
    question: "¿Para quién está pensado este template?",
    answer:
      "Está pensado para desarrolladores, creadores de contenido técnico y equipos que quieran publicar artículos rápidos, performantes y con control total del código.",
  },
  {
    question: "¿Usa Astro Content Collections?",
    answer:
      "Sí. Todo el contenido se gestiona mediante Content Collections, lo que garantiza tipado, validación y escalabilidad.",
  },
  {
    question: "¿Está optimizado para SEO y performance?",
    answer:
      "Absolutamente. El template prioriza HTML estático, carga progresiva y buenas prácticas SEO desde el primer render.",
  },
];
---
<section class="max-w-4xl py-15 max-2xl:px-0 max-xl:px-0 max-lg:px-4 max-sm:px-4">
      <header class="mb-10">
        <h2 class="text-2xl font-semibold text-white">Preguntas frecuentes</h2>
      </header>

      <div class="space-y-4">
        {faqs.map(({ question, answer }) => (
          <details
            class="group rounded-xl border border-slate-800 bg-slate-900 p-4"
          >
            <summary
              class="flex cursor-pointer list-none items-center justify-between"
          >
            <h3 class="text-lg font-medium text-white">
              {question}
            </h3>
            <span class="text-slate-400 transition group-open:rotate-180">
              ▼
            </span>
          </summary>

          <p class="mt-4 text-slate-400">
            {answer}
          </p>
        </details>
        ))}
      </div>
    </section>